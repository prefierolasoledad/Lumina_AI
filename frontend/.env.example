# Base URL the Next.js proxy (next.config.ts rewrites) forwards /api/* requests to.
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# WebSocket server URL (the backend shares this with its HTTP server).
NEXT_PUBLIC_WS_URL=ws://localhost:8080
